<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="actionMenuTextColor">@color/white</item>
        <item name="android:actionMenuTextColor">@color/whiteOpacity</item>

    </style>

    <style name="AppTheme.Toolbar" parent="AppTheme">
        <!--This line changes the color of text in Toolbar-->
        <item name="android:textColorPrimary">@color/whiteOpacity</item>
        <!--This line changes the color of icons in toolbar (back, overflow menu icons)-->
        <item name="android:textColorSecondary">@color/whiteOpacity</item>
    </style>

    <style name="AppTheme.WhiteAccent" parent="AppTheme">
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="AppTheme.Login" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/white</item>

        <item name="android:actionMenuTextColor">@color/whiteOpacity</item>

    </style>

</resources>
